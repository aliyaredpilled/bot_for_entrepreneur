# Техническое задание (ТЗ): Telegram Chat Archive Bot + AI Agent

## 1. Общие сведения
**Цель проекта:** Разработка Telegram-бота для автоматической архивации контента чатов и предоставления аналитических функций через AI-агента (Claude).
**Основной функционал:** Сохранение истории и медиа, анализ данных (Excel/CSV), построение графиков, ответы на вопросы по контексту переписки.

## 2. Технологический стек и окружение
*   **Язык:** Python 3.11+.
*   **Деплой:** Docker (docker-compose).
*   **Базовый образ:** Должен включать предустановленные библиотеки: `jq`, `pandas`, `matplotlib`, `seaborn`, `openpyxl`.
*   **AI Агент:** Claude Sonnet через **Claude Agent SDK** (`claude-agent-sdk`).
    *   SDK предоставляет готовые инструменты (Read, Grep, Glob, Bash).
    *   Встроенный стриминг ответов и вызовов инструментов.
    *   Поддержка сессий с сохранением контекста.
    *   Авторизация через OAuth-токен (переменная `CLAUDE_CODE_OAUTH_TOKEN`).
*   **Конфигурация:** Через переменные окружения (`.env`):
    *   `BOT_TOKEN` (Telegram)
    *   `CLAUDE_CODE_OAUTH_TOKEN` (AI SDK)
    *   `SESSION_TIMEOUT` (по умолч. 30 мин)

## 3. Требования к архитектуре данных
Система должна использовать файловую структуру для хранения данных, обеспечивая изоляцию между чатами. База данных не используется.

### 3.1. Структура директорий
Корневая директория `/app/chat_archive/` должна содержать подпапки для каждого чата:
```text
/chat_archive/chat_{id}/
├── history.txt      # Хронология переписки (Plain Text)
├── media/           # Файлы пользователей (входящие)
└── agent_files/     # Файлы, сгенерированные агентом (исходящие)
```

### 3.2. Формат хранения истории
Файл `history.txt` должен пополняться в реальном времени в формате:
```text
[DD.MM HH:MM] Имя_Пользователя: Текст сообщения
[DD.MM HH:MM] Имя_Пользователя: 📄 filename.ext → media/filename.ext
```
*   Для медиа-файлов указывать тип (иконку) и относительный путь.

## 4. Функциональные требования

### 4.1. Модуль архивации
Бот должен автоматически сохранять все входящие сообщения в папку текущего чата:
1.  **Текст:** Дописывается в `history.txt`.
2.  **Медиа:** Фото, видео, документы, голосовые, видео-кружки, стикеры сохраняются в папку `media/`.
3.  **Нейминг файлов:** Оригинальное имя + таймштамп (или генерация уникального имени, если оригинала нет), чтобы избежать перезаписи.
4.  **Системные события:** Логировать вход/выход участников, смену названия чата.

### 4.2. Модуль AI-агента
**Активация:** При упоминании `@bot_username` или реплае на сообщение бота.

**Управление сессиями:**
*   Создавать отдельную сессию для каждого `chat_id`.
*   Хранить контекст разговора.
*   **Таймаут:** Сбрасывать контекст (новая сессия) через 30 минут бездействия.
*   При смене сессии выводить лог (New Session / Continue Session).

**Системный промпт и инструменты:**
*   Динамически подставлять `chat_id` и пути к папкам текущего чата.
*   Агент должен иметь доступ к инструментам:
    *   `Read` (чтение файлов).
    *   `Grep` (поиск по паттернам в history.txt).
    *   `Glob` (поиск файлов).
    *   `Bash` (выполнение скриптов, использование python/pandas/matplotlib).

**Без ограничений на сложность:**
*   Не устанавливать жёсткие лимиты на количество шагов (`max_turns`) или стоимость запроса.
*   Агент должен выполнять задачи любой сложности до завершения.

### 4.3. Автоматическая отправка файлов
Реализовать парсер ответов агента для отправки вложений:
1.  Если в тексте ответа агента встречается путь к файлу (абсолютный, относительный, в backticks) — бот должен проверить наличие файла.
2.  Если файл существует — отправить его в чат отдельным сообщением.
3.  Определять тип отправки по расширению (изображение как фото, mp4 как видео, остальное как документ).
4.  **Маскировка путей:** В текстовом ответе для пользователя заменять длинные системные пути (`/app/chat_archive/...`) на короткие имена файлов.
5.  **Без дублирования имён:** Отправлять файлы без caption — имя файла и так видно в Telegram.

## 5. Требования к интерфейсу (UX)

### 5.1. Отображение прогресса (Real-time)
Вместо статичного "typing..." реализовать редактируемое сообщение со статусами:
1.  Начальный статус: `⏳ Секунду...`.
2.  Отображать текущее действие агента (инструмент или "мысль").
3.  **Задержка:** Минимальное время показа каждого статуса — **2 секунды** (для читаемости).
4.  Маппинг технических имен инструментов на человекочитаемые:
    *   `Grep` → `🔍 Ищу «...»`
    *   `Read` → `📖 Читаю файл...`
    *   `Bash` → `⚙️ Выполняю расчёты...`
    *   `Glob` → `📁 Сканирую файлы...`
5.  Помимо инструментов, показывать промежуточные реплики агента («Сейчас посмотрю...», «Нашёл!») как статусы.
6.  **Определение финального ответа:** Финальный ответ = последний `TextBlock` перед `ResultMessage`. Не фильтровать по длине.
7.  В финале сообщение заменяется на итоговый ответ агента. Промежуточные статусы удаляются.

### 5.2. Форматирование текста
Реализовать конвертер Markdown → Telegram HTML перед отправкой:
*   `**bold**` → `<b>bold</b>`
*   `_italic_` → `<i>italic</i>` (игнорировать `*` для курсива во избежание конфликтов).
*   Code blocks → `<pre>`.
*   Inline code → `<code>`.
*   `## Headers` → `<b>Headers</b>`.
*   Цитаты `> text` → `▎ text`.
*   Таблицы Markdown не поддерживать (агент должен использовать списки или файлы).

### 5.3. Стиль ответов агента
Настроить системный промпт агента так, чтобы он:
*   Давал **компактные** текстовые ответы (краткое резюме).
*   Выносил детальные данные в файлы (Excel, CSV, TXT).
*   **НЕ использовал markdown-таблицы** — вместо них списки с эмодзи.
*   Использовал эмодзи для структуры (📊💰📈).

## 6. Безопасность и ограничения
1.  **Изоляция:** Агент одного чата не должен иметь доступа к папкам других чатов.
2.  **Bash:** Ограничить рабочую директорию агента текущей папкой чата (по возможности).
3.  **API:** Токены не должны храниться в коде.
4.  **Размер файлов:** Учитывать лимит Telegram (50 MB) при отправке.

## 7. Логирование
Выводить в stdout структурированные логи:
*   События сессий (Start/Expiry).
*   Текст запроса (первые 100 символов).
*   Цепочка вызовов инструментов.
*   Статистика токенов (input/output) и примерная стоимость запроса.

---

### Приемка работы (Acceptance Criteria)
1.  Бот успешно запускается через `docker-compose up`.
2.  История переписки сохраняется в `history.txt` в читаемом виде.
3.  При запросе "Построй график из report.xlsx" бот находит файл, генерирует изображение и присылает его в чат.
4.  Статусы работы («Ищу», «Читаю», промежуточные реплики) сменяются плавно, не чаще 1 раза в 2 секунды.
5.  Спустя 35 минут молчания бот "забывает" контекст прошлого разговора.
6.  Файлы агента сохраняются в `agent_files/`, файлы пользователей — в `media/`.
7.  Отправленные файлы не имеют дублирующего caption с именем.
8.  Длинные пути в ответе заменяются на короткие имена файлов.