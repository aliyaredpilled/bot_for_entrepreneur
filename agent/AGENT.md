# Agent Instructions

Привет! Добро пожаловать в проект Telegram AI Bot.

Здесь ты найдёшь всё необходимое для работы. Не торопись, внимательно читай документацию, и всё получится!

---

## Воркфлоу

### 1. Ознакомься с проектом

Сначала осмотрись — пойми что уже сделано и что предстоит:

```bash
# Структура проекта
ls -la

# Текущий прогресс — что уже готово?
cat agent/progress.txt

# Техническое задание — что мы строим?
cat agent/product_description.md

# Список фич и BDD тесты — конкретные задачи
cat agent/FEATURES.md

# Советы по Claude Agent SDK — пригодится для AI-агента
cat agent/claude_agent_sdk_tips.md

# Советы по Telethon — для тестирования
cat agent/telegram_userbot_tips.md
```

### 2. Выбери задачу

1. Открой `agent/FEATURES.md`
2. Найди задачу со статусом `[ ]` (TODO)
3. Выбери задачу по приоритету:
   - Сначала инфраструктура (8.1, 8.2 — Docker)
   - Потом базовые модули (1-4)
   - Затем UX и остальное (5-7, 9)
4. Учитывай зависимости — некоторые задачи требуют других

### 3. Начни работу

1. **Поменяй статус на `[~]`** (IN_PROGRESS) в `agent/FEATURES.md`
2. Обнови `agent/progress.txt` — укажи над чем работаешь
3. Закоммить изменение статуса:
   ```bash
   git add agent/FEATURES.md agent/progress.txt
   git commit -m "Start work on: <номер> <название>"
   # Пример: git commit -m "Start work on: 8.1 Dockerfile с зависимостями"
   ```

### 4. Реализуй фичу

- Работай в Docker-контейнере
- Используй `docker compose` (без тире!)
- Следуй BDD сценариям из `agent/FEATURES.md`
- Сверяйся с ТЗ в `agent/product_description.md`

```bash
# Сборка
docker compose build

# Запуск
docker compose up -d

# Логи
docker compose logs -f

# Остановка
docker compose down
```

### 5. Протестируй

Тестирование обязательно! Но способ зависит от типа задачи:

#### Инфраструктурные задачи (Docker, логирование)

Для задач **8.1, 8.2 (Docker)** и **9.1 (Логирование)**:

```bash
# Проверь что контейнер запускается
docker compose up -d
docker compose ps  # статус running?

# Проверь логи на ошибки
docker compose logs

# Для логирования — убедись что логи пишутся в stdout
docker compose logs -f bot
```

**Что проверить:**
- [ ] Контейнер запускается без ошибок
- [ ] Статус `running` в `docker compose ps`
- [ ] Логи пишутся корректно

#### Задачи с Telegram-взаимодействием

Для всех остальных задач (архивация, агент, сессии, UX) нужен **userbot_tester**:

```bash
# Запусти userbot тестер
cd userbot_tester
docker compose --profile testing up -d

# Отправь тестовое сообщение
./send.sh "@bot <тестовый запрос>"

# Проверь логи
docker logs -f telegram-userbot-tester

# Или читай файл логов
cat logs/log_$(date +%Y%m%d).txt
```

**Что проверить (по BDD сценарию задачи):**
- [ ] Ожидаемое поведение из Gherkin-сценария
- [ ] Бот ответил (`🤖 БОТ` в логах) — если задача про ответы
- [ ] Статусы менялись (`✏️ БОТ EDIT`) — если задача про UX
- [ ] Файлы отправлены — если задача про файлы
- [ ] Нет ошибок в логах бота

#### Unit-тесты (опционально)

Для изолированных функций (например, 7.1 Форматирование) можно написать unit-тест:

```python
# tests/test_formatter.py
def test_bold_conversion():
    assert md_to_html("**bold**") == "<b>bold</b>"
```

### 6. Заверши задачу

Только после успешного теста:

1. **Поменяй статус на `[x]`** (DONE) в `agent/FEATURES.md`
2. Обнови таблицу прогресса внизу `agent/FEATURES.md`
3. Обнови `agent/progress.txt`
4. Закоммить и запушь:
   ```bash
   git add -A
   git commit -m "Complete: <номер> <название>

   - Что сделано
   - Как протестировано

   Co-Authored-By: Claude <agent@anthropic.com>"

   git push

   # Пример:
   # git commit -m "Complete: 8.1 Dockerfile с зависимостями
   #
   # - Создан Dockerfile с Python 3.11, pandas, matplotlib
   # - Проверено: docker compose up работает, контейнер running
   #
   # Co-Authored-By: Claude <agent@anthropic.com>"
   ```

---

## Правила

### Git
- Коммить после каждой завершённой задачи
- Пиши понятные commit messages
- Всегда пушь в remote

### Docker
- Используй `docker compose` (НЕ `docker-compose`!)
- Работай внутри контейнеров
- Не забывай про volumes для данных

### Тестирование
- **Не ставь `[x]` без теста** — это важно!
- Выбирай способ теста по типу задачи (см. раздел 5)
- Проверяй по BDD сценарию из FEATURES.md

### Код
- Следуй стилю существующего кода
- Не ломай работающий функционал
- Документируй сложные места

---

## Структура проекта

```
bot_for_entrepreneur/
├── agent/                       # Документация для агентов
│   ├── AGENT.md                 # ЭТА ИНСТРУКЦИЯ
│   ├── FEATURES.md              # Фичи и BDD тесты
│   ├── progress.txt             # Текущий статус
│   ├── product_description.md   # Техническое задание
│   ├── claude_agent_sdk_tips.md # Советы по SDK
│   └── telegram_userbot_tips.md # Советы по Telethon
│
├── src/                         # Исходный код бота (создать)
│   ├── bot.py
│   ├── agent.py
│   ├── archiver.py
│   └── ...
│
├── userbot_tester/              # Тестер
│   ├── userbot.py
│   ├── docker-compose.yml
│   └── ...
│
├── .env                         # Секреты (не коммитить!)
├── .gitignore
├── Dockerfile                   # Образ бота (создать)
└── docker-compose.yml           # Оркестрация (создать)
```

---

## Приоритет задач

1. **Docker setup** (8.1, 8.2) — без этого ничего не запустить
2. **Архивация текста** (1.1-1.3) — базовый функционал
3. **AI-агент базовый** (3.1-3.2) — интеграция Claude SDK
4. **Сессии** (4.1-4.3) — память между сообщениями
5. **Остальное** — по порядку в agent/FEATURES.md

---

## Полезные команды

```bash
# Статус git
git status

# История коммитов
git log --oneline -5

# Пересобрать без кеша
docker compose build --no-cache

# Зайти в контейнер
docker compose exec bot bash

# Очистить всё
docker compose down -v --rmi all
```

---

Удачи! Если что-то непонятно — перечитай документацию, там есть ответы на большинство вопросов.
